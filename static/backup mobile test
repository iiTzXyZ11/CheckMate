
.custom-file-upload {
    padding: 10px 18px;
    border: 2px solid #000; /* Border color */
    border-radius: 10px;
    color: black; /* Text color */
    background-color: #fff; /* Background color */
    text-decoration: none;
    cursor: pointer;
    font-weight: bold;
    margin: 15px 0;
    display: flex;
    justify-content: center;
    align-items: center;
    transition: background-color 0.3s, border-color 0.3s; /* Smooth transition */
}

.custom-file-upload:hover {
    background-color: black; /* Hover background color */
    color: white; /* Hover text color */
    border-color: transparent; /* Hover border color */
}

.content-wrapper {
    display: grid;
    grid-template-columns:2 ;
    background-color: yellow;
}

.container {
    font-family: 'Oswald'; /* Applied to all children of .container */
    display: grid;
    gap: 5px;
    grid-template-columns: repeat(1, minmax(100px, 1fr));
    background: rgb(255, 0, 0);
    width: 100vw; /* Full width of viewport */
    height: 10vh; /* Full height of viewport */
    margin: 10vh;
    border: 3px solid rebeccapurple;
    border-radius: 20px;
    font-size: clamp(0.1rem, 3vw, 1.1rem);
}


.box, .box1 {
    padding: 0 1vw; /* Uniform padding */
    background: aqua;
    margin: 1rem;
}


.container textarea {
    height: clamp(200px, 5vh, 800px);
    width: 100%; /* Fill width */
    resize: none; /* Prevent resizing */
    box-sizing: border-box; /* Include padding and border in total width/height */
    padding: clamp(5px, 2vw, 10px); /* Responsive padding */
    border-radius: 10px; /* Rounded corners */
    font-size: clamp(0.1rem, 3vw, 1.1rem);
}

.container button {
    width: 100%; /* Fill width */
    padding: clamp(5px, 2vw, 10px); /* Responsive padding */
    border-radius: 10px; /* Rounded corners */
    font-size: clamp(0.1rem, 3vw, 1.1rem);
    box-sizing: border-box; /* Include padding and border in total width/height */
}

.box h2, .box1 h2 {
    color: black;
}

.container p,
.container form {
    margin: clamp(5px, 1vh, 10px) 0; /* Responsive margin */
}

/* Tips Container */
.tips-container {
    position: relative; /* Enables absolute positioning for child elements */
    display: flex;
    flex-direction: column; /* Stacks content vertically */
    align-items: flex-start; /* Aligns content to the left by default */
    background-color: #151515;
    border: 3px solid white;
    width: 50%;
    max-width: 46%;
    margin: 10vh 1vw 0 1vw;
    padding: 1vh 1vw 0 1vw;
    border-radius: 20px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

/* Tips Header */
.tips-header {
    display: flex;
    align-self: center;
    justify-content: center;
    align-items: center;
}

.tips-container h1 {
    display: inline-block;
    font-weight: bold;
    color: white;
    margin: 0;
    font-size: clamp(1rem, 3vw, 4rem);
}

/* Tips Images */
.tips-header img {
    padding: 0;
    margin-bottom: 0;
    margin-right: 0.5vw;
    height: auto;
    width: clamp(5rem, 8%, 10rem);
}

/* Tips Text */
.tips {
    display: grid;
    gap: 2px;
    grid-template-columns: 32vw 12vw; /* First column is wider */
    grid-auto-rows: clamp(25vh,25vh,50vh); /* Distribute height equally among rows */
    background:white;
    color:white;
}

.tip1, .tip3, .tip5 {
    display: flex;
    align-items: center;
    font-size: clamp(0.5rem, 1vw, 2rem);
    text-align: justify; /* Center text horizontally */
    background-color: #151515;
    padding: 0 1vw;
    flex-direction: column;
    justify-content: center;
}

.tip2, .tip4, .tip6 {
    display: flex;
    justify-content: center;
    align-items: center;
    background-color: #151515;
}

.tip2 img, .tip4 img, .tip6 img{
    width: 10vw;
}


    
